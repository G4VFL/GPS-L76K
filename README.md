The file on the Waveshare WEB site for the L76K device uses the old L76x.py

This does not work with the L76K device which uses a diffeent set of commands to setup the GPS receiver.

I have altered the L76x file to L76K. Essentially it is the same are the old file but with the new commads. This is to minimise the code changes elsewhere in my program.

I use import L76K rather than L76x in my other pieces of code.

73 de G4VFL
